<UserControl
    x:Class="SampleApp.Views.ComponentListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:SampleApp.Resources"
    xmlns:viewModels="clr-namespace:SampleApp.ViewModels"
    MinWidth="50"
    d:DataContext="{d:DesignInstance viewModels:ComponentListViewModel}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/CommonPartsDictionary.xaml" />
                <ResourceDictionary Source="../Resources/ComponentDataTemplates.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border
                Grid.Column="0"
                Height="20"
                Margin="2,0,4,0"
                Padding="2,0,2,0"
                BorderBrush="#ABADB3"
                BorderThickness="1,1,1,1">
                <DockPanel LastChildFill="True">

                    <ContentControl
                        Width="15"
                        Height="15"
                        Margin="0,0,2,1"
                        VerticalAlignment="Center"
                        DockPanel.Dock="Left"
                        Template="{StaticResource FunnelIcon}" />

                    <Button
                        Width="14"
                        Height="14"
                        Command="{Binding ResetFilterCommand}"
                        DockPanel.Dock="Right"
                        Style="{StaticResource ResetButtonStyle}" />

                    <TextBox
                        VerticalContentAlignment="Center"
                        BorderThickness="0,0,0,0"
                        FontSize="12"
                        Text="{Binding Filter.Value, UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>
            </Border>



            <Button
                Grid.Column="1"
                Width="20"
                Height="20"
                Margin="0,2,2,2"
                Padding="2,2,2,2"
                HorizontalAlignment="Right"
                Command="{Binding OpenComponentCreationDialogCommand}">
                <Viewbox>
                    <Path
                        Data="M 0 5 L 10 5 M 5 0 L 5 10"
                        Stroke="Black"
                        StrokeThickness="1" />
                </Viewbox>
                <Button.ContextMenu>
                    <ContextMenu>
                        <MenuItem Command="{Binding CreateDummyComponentsCommand}" Header="{x:Static resources:StringResources.ComponentCreationButton_ContextMenu_CreateDummyComponents}" />
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
        </Grid>

        <Separator
            Grid.Row="1"
            Margin="0,2,0,0"
            Background="Black" />

        <ItemsControl
            Grid.Row="2"
            ItemsSource="{Binding ComponentsView}"
            ScrollViewer.CanContentScroll="True"
            VirtualizingPanel.ScrollUnit="Pixel"
            VirtualizingStackPanel.IsVirtualizing="False">

            <ItemsControl.Template>
                <ControlTemplate>
                    <ScrollViewer
                        VerticalAlignment="Stretch"
                        HorizontalScrollBarVisibility="Disabled"
                        VerticalScrollBarVisibility="Visible">
                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                    </ScrollViewer>
                </ControlTemplate>
            </ItemsControl.Template>

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ContentPresenter VerticalAlignment="Center" Content="{Binding .}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
